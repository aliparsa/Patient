<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".Fragments.PatientInfo">


    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/profilePicturePath"
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:hint="profilePicturePath"
            android:src="@drawable/ic_add_image"
            />

        <LinearLayout
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <EditText
                android:id="@+id/name"
                android:layout_width="fill_parent"
                android:layout_height="wrap_content"
                android:hint="name" />

            <EditText
                android:id="@+id/dateOfBirth"
                android:layout_width="fill_parent"
                android:layout_height="wrap_content"
                android:hint="dateOfBirth"
                android:inputType="date" />


        </LinearLayout>

    </LinearLayout>

    <EditText
        android:id="@+id/nationalCode"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:hint="nationalCode"
        android:inputType="number" />

    <EditText
        android:id="@+id/PhoneNumber"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:hint="PhoneNumber"
        android:inputType="phone" />

    <Button
        android:id="@+id/addMedical"
        android:layout_width="fill_parent"
        android:layout_height="66dp"
        android:hint="addMedical"
        android:text="save and add medical info" />

</LinearLayout>
